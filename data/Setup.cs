using System;
using System.Collections.Generic;
using System.Drawing;
using System.Linq;
using System.Text;

namespace ConsoleGame.Setup
{
    public class GameSetup
    {
        public static void Setup()
        {
            // here you do all the things when you would set up your game
            // tuning the settings to your liking
            // loading your navy boat models
            // loading terrain dictionary
            // loading game board sea dimensions
            // Open settings or proceed with defaults
                        // which game version 1990 Milton Bradley version or 2002 Hasbro version
                        // Tune Game Board (sea) dimensions (10-26)
                        // Specify the max turn before game end (sum across boats - SEA_SIZE*SEA_SIZE)
                        // controlls section e for exit game or c for exiting choosing
                        // (optional) add more boat models
                        // (optional) add more sea options (eg partially destroyed)
        }
    }
}